// This imports defineConfig from Vite.
// defineConfig helps us write the Vite configuration in a clear way.
import { defineConfig } from "vite";

// This exports the Vite configuration for the frontend project.
export default defineConfig({
  // This object is currently empty, which means Vite will use its default settings.
});
